type Query {
  """
  Returns a greeting. Use for a quick sanity check.
  """
  hello(name: String): String!
  """
  Simple ping to verify GraphQL is up.
  """
  ping: String!
}

type Mutation {
  """
  Echo back a message (for testing mutations).
  """
  echo(message: String!): String!
}
