<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
  <!-- Use Spring Boot default colored pattern (faint date, level colors, cyan logger) -->
  <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
  </root>
  <!-- Application logs (controllers, services, etc.) -->
  <logger name="com.mafauser.service" level="INFO"/>
  <!-- Request logging from RequestLoggingFilter -->
  <logger name="com.mafauser.service.config.RequestLoggingFilter" level="INFO"/>
</configuration>
